
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: csiaddonsjobs.csiaddons.openshift.io
spec:
  group: csiaddons.openshift.io
  names:
    kind: CSIAddonsJob
    listKind: CSIAddonsJobList
    plural: csiaddonsjobs
    singular: csiaddonsjob
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.namespace
      name: namespace
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - jsonPath: .spec.operation.name
      name: operationName
      type: string
    - jsonPath: .spec.operation.dataSource.kind
      name: dataSourceKind
      type: string
    - jsonPath: .spec.operation.dataSource.name
      name: dataSourceName
      type: string
    - jsonPath: .status.state
      name: state
      type: string
    - jsonPath: .status.message
      name: message
      type: string
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: CSIAddonsJob is the Schema for the csiaddonsjobs API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: CSIAddonsJobSpec defines the desired state of CSIAddonsJob
            properties:
              activeDeadlineSeconds:
                description: ActiveDeadlineSeconds specifies the duration in seconds
                  relative to the startTime that the operation might be active; value
                  must be positive integer.
                format: int64
                type: integer
              backOffLimit:
                description: BackOffLimit Specifies the number of retries before marking
                  this operation failed.
                format: int32
                type: integer
              operation:
                description: Operation represents the operation to be performed on
                  the volume. Current Supported operation is "ReclaimSapce".
                properties:
                  dataSource:
                    description: DataSource represents the object associated with
                      the volume.
                    properties:
                      apiGroup:
                        description: APIGroup is the group for the resource being
                          referenced. If APIGroup is not specified, the specified
                          Kind must be in the core API group. For any other third-party
                          types, APIGroup is required.
                        type: string
                      kind:
                        description: Kind is the type of resource being referenced
                        type: string
                      name:
                        description: Name is the name of resource being referenced
                        type: string
                    required:
                    - kind
                    - name
                    type: object
                  name:
                    description: Name repesents operation name.
                    type: string
                  parameters:
                    additionalProperties:
                      type: string
                    description: Parameters is a key-value map with driver configurations
                      for performing the operation.
                    type: object
                required:
                - dataSource
                - name
                type: object
            required:
            - operation
            type: object
          status:
            description: CSIAddonsJobStatus defines the observed state of CSIAddonsJob
            properties:
              message:
                description: Message contains any message from the operation.
                type: string
              state:
                description: State defines the state of the operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
